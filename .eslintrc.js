module.exports = {
	extends: ['@wordpress/eslint-plugin'],
	rules: {
		'array-bracket-spacing': ['error', 'always'], // Enforces space inside `[]`
		'space-in-parens': ['error', 'always'], // Enforces space inside `()`
	},
};
